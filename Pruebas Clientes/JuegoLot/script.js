function combinacion() {
    let numerosArrays = [];
    let numero;
    let combinacion = [];

    for (let i = 0; i < 6; i++) {
        numero = Math.floor(Math.random() * (49 - 1) + 1);
        flag = false;
        while (condition) {
            if (numero.includes(numerosArrays)) {
                
            }
        }
        
    }

}